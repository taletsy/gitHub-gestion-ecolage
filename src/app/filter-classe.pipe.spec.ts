import { FilterClassePipe } from './filter-classe.pipe';

describe('FilterClassePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClassePipe();
    expect(pipe).toBeTruthy();
  });
});
